{
    "output_dir": "results/multi_test",
    "technique": {
        "name": "sld",
        "config": {
            "device": "cuda",
            "num_inference_steps": 25
        }
    },
    "metrics": [
        {
            "name": "asr",
            "config": {
                "use_nudenet": true,
                "device": "cuda",
                "limit": 3
            }
        },
        {
            "name": "clip_score",
            "config": {
                "device": "cuda",
                "limit": 3
            }
        }
    ]
}
